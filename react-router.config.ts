export default {
  // Server-side render by default, to enable SPA mode set this to `false`
  ssr: true,
  // PostCSS support
  postcss: true,
  // Ignore route files that start with a dot
  ignoredRouteFiles: ['**/.*'],
};
